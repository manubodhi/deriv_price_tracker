/// Path of all the image assets in defined here
/// Add image to assets/images at root and then add the path here.

const _basePath = 'assets/images';

class Images {
  Images._();
  static const derivLogo = '$_basePath/logo.png';
}
